-if current_user == nil
  %h1
    = t('main_page.welcome_title')
  %p
    = t('main_page.welcome_text')
-else
  = render :partial => "common/main_page_user", :locals => {:user => current_user, :invalid => @invalid, :critical => @critical, :ends => @ends, :full => @full}

