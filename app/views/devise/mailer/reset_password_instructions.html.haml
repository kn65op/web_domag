%p
	= (t 'account.mail.welcome') + " " + @resource.email

%p
	= t 'account.mail.reset_password_instructions'

%p
	= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)
%p
	= t 'account.mail.ignore'
%p
	= t 'account.mail.no_password_change'