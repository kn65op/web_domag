%h1= t('things.edit.title')

= simple_form_for @thing, :url => url_for(edit_thing_path)  do |f|
  = f.input :name
  = f.input :description
  = f.input :unit
  = f.association :category, :collection => @categories
  = f.button :submit
